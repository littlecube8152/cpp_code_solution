#include <bits/stdc++.h>
#define ll long long
#define pll pair<ll, ll>
#define F first
#define S second
using namespace std;

ll Q, a[1000005], ans, idx;

signed main()
{
    ios::sync_with_stdio(0), cin.tie(0);
    ofstream cout("a.txt");
    for (int i = 1; i <= 40; i++)
        for (int j = i + 1; j <= 40; j++)
            cout << i << ' ' << j << '\n';
}

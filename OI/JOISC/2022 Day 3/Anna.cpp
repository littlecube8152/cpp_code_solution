#include <bits/stdc++.h>
using namespace std;

int Declare()
{
	return 2000;
}

pair<vector<int>, vector<int>> Anna(int A)
{
	int q = A / 2000, r = A % 2000;
	if((q + r) % 2 == 1)
	{
		if(r <= 1)
			q -=
	}
}
